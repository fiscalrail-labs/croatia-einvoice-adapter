{
  "artifact_version": "0.3.0",
  "status": "production_release_candidate",
  "production_ready_is_dynamic": true,
  "completed": [
    "FastAPI service and public Render deployment architecture",
    "JSON-to-UBL generation through a pinned EN 16931 builder",
    "official Croatian UBL 2.1 XSD acquisition during Docker build",
    "EN 16931 Schematron validation",
    "Croatian CIUS/EXT Schematron validation",
    "normalized machine-readable error responses",
    "persistent validation worker",
    "direct API-key and RapidAPI proxy-secret protection",
    "request-size enforcement and secure XML parsing",
    "readiness gate that fails closed in production",
    "weekly Docker integration workflow",
    "RapidAPI listing and pricing draft"
  ],
  "locally_verified": {
    "python_tests": 11,
    "python_compile": "passed",
    "node_worker_syntax": "passed"
  },
  "deployment_verification_required": [
    "Docker build downloads the current official Croatian XSD archive",
    "npm installs pinned verifaktura packages from the public registry",
    "GET /ready returns HTTP 200 and production_ready true",
    "production smoke test validates examples/invoice.json with zero fatal findings"
  ],
  "commercial_guardrail": "Do not enable paid plans unless /ready is green and the production integration test passes."
}
